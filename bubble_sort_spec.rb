#Bubble-sort Algorithm

require "bubble_sort"

describe "#bubble-sort" do
	
	it "compares the first value to the second value in an array to determine if first value is greater" do
		array = bubble-sort([3, 4])
		expect(array).to_eq(false)
	end
end